<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AdamsPizzaria" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AdamsPizzaria" parent="Base.Theme.AdamsPizzaria" />

    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/logo</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splashlogo</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.AdamsPizzaria</item>
    </style>
</resources>